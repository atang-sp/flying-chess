# Flying Chess

本项目默认描述同场、纯本地的惩罚飞行棋。`classic` 保留既有完整规则，`party` 在独立局内状态中增加轻量决定权、同场反应与可见节奏。用户显式进入的“联机升温局”是独立联网入口，不改变经典局、本地升温局或局域网手柄的本地优先边界。

## 本局玩法

**本局玩法**：
玩家在一局开始前选择的完整玩法规则；当前可选经典局或升温局。
_Avoid_: 模式、版本

**经典局**：
沿用 `classic_v1` 规则集的既有完整玩法，其开局、移动、惩罚与胜利行为保持冻结。
_Avoid_: 老模式、普通模式

**升温局**：
沿用 `party_v2` 规则集的实验玩法，目标时长约二十分钟，并包含三幕、干预筹码、惩罚变体、终局奖惩和同场反应。
_Avoid_: 派对模式、主持人模式

**联机升温局**：
用户显式进入的 3–8 人联网承载方式；每位玩家通过 WebSocket 连接房间服务器，公共局面由服务器权威结算，私密决定按玩家投影。房间只存于服务器内存，创建后最长两小时，服务重启即结束；它仍使用 `party_v2`，不是新的规则集。
_Avoid_: 默认玩法、局域网手柄、云端账号

**规则集**：
定义一局玩法行为的稳定规则身份；应用版本变化不自动改变规则集。
玩法缓存必须同时保存玩法与规则集版本；没有版本的旧升温局选择不得静默升级，必须回退经典局并重新选择。
_Avoid_: 应用版本、配置

## 升温局

**幕**：
升温局可见的节奏阶段，依次为暖场、升温和终局。
_Avoid_: 等级、关卡

**轮**：
每位玩家各获得一次回合的公平周期；因休息而跳过的回合仍计入本轮。
_Avoid_: 单个回合

**活跃时长**：
升温局从实际开始后累计的非暂停时间，用于三幕推进和约二十分钟的结束规则。
_Avoid_: 页面停留时长

**干预筹码**：
每位玩家在升温局中持有的两次通用决定权，可用于重掷、随机二选一、转嫁、加码或免疫；每回合最多消耗一枚。
_Avoid_: 货币、积分

**反应机会**：
每轮一次、由非当前玩家在指定玩家的回合中完成的短决定；猜中骰子范围后可保留或镜像点数。
_Avoid_: 主持、旁观操作

**镜像点数**：
将本次骰点替换为七减去当前骰点；同一次掷骰最多被改变一次。
_Avoid_: 重掷

**随机二选一**：
当前玩家消耗干预筹码后，在两个兼容的普通静态棋盘惩罚结果中选择一个，再交由既有规则解析。
_Avoid_: 修改惩罚配置

**惩罚变体**：
升温幕与终局幕随机附加的执行规则；盲盒、条件、延迟和双向四种核心变体始终可用，跨局进度可额外解锁返场。条件结果、延迟队列、双向交换与返场减半都由应用明确结算。
_Avoid_: 仅供参考的提示文案

**终局奖惩**：
升温局开局前可配置的胜者动作与败者次数；启用梯度后按落后进度档递增，并列玩家同档。
_Avoid_: 经典局胜利规则

**事件卡**：
独立于棋盘格的可导入卡牌；由每 N 回合、连续惩罚或特定骰点触发，并可产生逐人投票计票、全员猜拳、小游戏、临时倍率或玩家绑定。
_Avoid_: 机关文案

**局域网手柄**：
手机通过原生 WebRTC DataChannel 接收自己的私密投影并提交骰子、反应与干预；邀请与应答在两块屏幕间手工交换，不使用默认云端信令或外部中继。
_Avoid_: 联网对战、共享私密状态、PeerJS 云端房间

**临时规则**：
事件卡激活后按剩余回合数持续生效的惩罚倍率或绑定关系；由事件状态机递减并进入规则解析。
_Avoid_: 永久配置修改

**小游戏机关**：
以反应速度、记忆翻牌或快速问答完成交互闭环的机关；结果产生一次性免罚或下一次惩罚加倍。
_Avoid_: 只展示说明的机关

**社区配置包**：
通过静态目录或 HTTP(S) JSON 加载的版本化内容包，可包含事件卡、终局奖惩或 Party Studio 场景；响应按流限制为 500KB，并在应用前完整校验。
_Avoid_: 远程脚本、账号市场

**本地进度**：
仅保存在当前设备的跨局累计数据，包括完成局数、受罚、求饶、连锁、成就与耻辱墙；不进入匿名遥测。
_Avoid_: 云端账号、公开排行榜

**Party Studio**：
升温局开局前显式启用的自定义场景，统一定义幕数和时间门控、格子比例、Q&A/Dare 内容池、拖拽布局与主题。
_Avoid_: 修改经典局、内置预设覆盖

**本地高光卡**：
升温局结束后仅在当前设备生成的非敏感局内摘要；它不进入匿名遥测，也不包含惩罚正文或玩家身份。
_Avoid_: 战绩、分享卡
