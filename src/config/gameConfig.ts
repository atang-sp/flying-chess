// The canonical game configuration lives in the framework-free game-core
// package so the browser, local Party flow, and room server cannot drift.
export {
  CELL_COLORS,
  CELL_ICONS,
  CELL_ICON_NAMES,
  GAME_CONFIG,
  PARTY_BOARD_CONFIG,
  STANDARD_BOARD_CONFIG,
  STANDARD_PUNISHMENT_CONFIG,
} from '@flying-chess/game-core/config'
